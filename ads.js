
function openAdConfirm(type,minerId=null){pendingAdType=type;pendingMinerId=minerId;document.getElementById('adConfirmModal').classList.add('active')}
function closeAdConfirm(){pendingAdType=null;pendingMinerId=null;document.getElementById('adConfirmModal').classList.remove('active')}
function proceedAd(){const type=pendingAdType;const minerId=pendingMinerId;closeAdConfirm();if(type==='giga'){window.showGiga().then(()=>{giveAdReward(()=>{coins+=REWARD_PER_AD;gigaWatched+=1;trackTotalAds();localStorage.setItem('coins',coins);localStorage.setItem('gigaWatched',gigaWatched);showToast("+2 APU Earned!",'success')})}).catch(()=>showToast("No ads available",'error'))}
if(type==='monetag'){window.show_11258922().then(()=>{giveAdReward(()=>{coins+=REWARD_PER_AD;monetagWatched+=1;trackTotalAds();localStorage.setItem('coins',coins);localStorage.setItem('monetagWatched',monetagWatched);showToast("+2 APU Earned!",'success')})}).catch(()=>showToast("No ads available",'error'))}
if(type==='miner'){const data=minerData[minerId];window.showGiga().then(()=>{giveAdReward(()=>{data.adsWatched+=1;totalAdsWatched+=1;localStorage.setItem(`miner${minerId}_adsWatched`,data.adsWatched);localStorage.setItem('totalAdsWatched',totalAdsWatched);coins+=REWARD_PER_AD;localStorage.setItem('coins',coins);showToast(`+${REWARD_PER_AD} APU! ${2-data.adsWatched} left`,'success')})}).catch(()=>showToast("No ads available",'error'))}}
function checkCooldown(){const now=Date.now();const timeLeft=COOLDOWN_TIME-(now-lastAdTime);const onCooldown=timeLeft>0;document.getElementById('gigaBtn').disabled=onCooldown||gigaWatched>=DAILY_LIMIT;document.getElementById('monetagBtn').disabled=onCooldown||monetagWatched>=DAILY_LIMIT;if(onCooldown){const secs=Math.ceil(timeLeft/1e3);document.getElementById('gigaBtn').innerText=`Wait ${secs}s`;document.getElementById('monetagBtn').innerText=`Wait ${secs}s`}else{document.getElementById('gigaBtn').innerText='Watch';document.getElementById('monetagBtn').innerText='Watch'}}
document.getElementById('gigaBtn').onclick=function(){const now=Date.now();if((COOLDOWN_TIME-(now-lastAdTime))>0)return showToast("Wait 5 seconds",'warning');if(gigaWatched>=DAILY_LIMIT)return showToast("Giga limit reached",'error');openAdConfirm('giga')};
document.getElementById('monetagBtn').onclick=function(){const now=Date.now();if((COOLDOWN_TIME-(now-lastAdTime))>0)return showToast("Wait 5 seconds",'warning');if(monetagWatched>=DAILY_LIMIT)return showToast("Monetag limit reached",'error');openAdConfirm('monetag')};